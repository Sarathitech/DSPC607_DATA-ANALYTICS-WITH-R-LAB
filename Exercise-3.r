#Ex3a. Create a data frame called student data, explore the structure of the data and process it using data.frame.

sdata <- data.frame(sname = c("ragu","vicky","vel"),srollno=c(12,41,05),sbranch=c("ds","ds","ds"),cgpa=c(8.85,9.12,7.45))
sdata

head(sdata)
nrow(sdata)
ncol(sdata)
